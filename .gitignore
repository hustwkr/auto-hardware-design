*.pyc
__pycache__/
.DS_Store
Thumbs.db
node_modules/
.vscode/
.env
.auth_salt  # Auto-generated scrypt salt (per-instance)
*.log

# Large reference files (keep in repo but don't track changes accidentally)
standards/*.pdf

# Sensitive config — contains real credentials in production, template only in repo
backend/email.config.json

# User feedback data — must not be committed
backend/feedback.json
backend/feedback_*.txt

# Backup versions - not needed for production
v2/

# Build temp files
build_temp_*.html
*.tmp

# Fix scripts, build temp, and backups (dev artifacts)
_fix.js
scripts/_fix_all.js
scripts/apply_*.cjs
_apply_fixes_*.cjs
fix_*.py
build*.py
*.bak
backend/*.bak

# Accidental nested copies
codexprojects/

# Token secret (auto-generated, per-instance)
backend/.token_secret

# Test coverage reports
coverage/
.nyc_output/

# Claude Code settings — contains tokens/passwords
.claude/
